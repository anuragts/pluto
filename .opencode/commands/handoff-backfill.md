---
description: Import recent local OpenCode sessions into .handoffs
---

Interpret `$ARGUMENTS` as the optional number of recent sessions to import.

Call the `handoff_backfill` tool exactly once.

Then report:

- imported session IDs
- skipped session IDs
- warnings
- next useful `get_context` command to run
