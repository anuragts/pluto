{
  "$schema": "https://opencode.ai/config.json"
  // Handoff backfill is manual by default so OpenCode startup stays fast.
  // Use /handoff-backfill when you want to import history.
  // If you want the old startup behavior temporarily:
  // OPENCODE_HANDOFFS_AUTO_BACKFILL=1 opencode .
}
